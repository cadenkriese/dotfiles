# Path
export PATH="/opt/homebrew/bin:$PATH"
export PATH="/opt/homebrew/sbin:$PATH"
export PATH="/opt/homebrew/opt/ruby/bin:$PATH"
export PATH="/opt/homebrew/lib/ruby/gems/3.2.0/bin:$PATH"
export PATH="/opt/homebrew/opt/python@3.11/libexec/bin:$PATH"
export PATH="$HOME/Library/flutter/bin:$PATH"
export PATH="$HOME/.pub-cache/bin:$PATH"

export ANDROID_SDK_ROOT="/opt/homebrew/share/android-commandlinetools"
if [ -d $ANDROID_SDK_ROOT ]
then
  PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/emulator:$ANDROID_SDK_ROOT/platform-tools:$PATH"
fi

export ZSH_COMPDUMP=$HOME/cache/.zcompdump-$HOST
export EDITOR="cot -w"

# Aliases
alias cat="bat"
alias ls="exa --color=auto"
alias md="open /Applications/Markdown\ Editor.app/"
# '\'' inserts a single literal ', \' needs to be enclosed in '' to escape and enter the string
# There is an extra ' at the end to close the
alias pfd='osascript -e '\''tell application "Finder" to return POSIX path of (insertion location as alias)'\'
alias cdf='cd $(pfd)'

# History cleaning
setopt HIST_IGNORE_SPACE

# ZSH Syntax highlighting
source /opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
# z.lua Folder Navigation
eval "$(lua $(brew --prefix z.lua)/share/z.lua/z.lua --init zsh)"

eval "$(starship init zsh)"
